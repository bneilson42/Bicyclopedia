<div class="form-group">
  <%= form_for @collaboration do |f| %>
    <%= f.hidden_field :wiki_id, value: @wiki.id %>
    <%= f.select :user_id, options_for_select(@possible_collaborators) %>
    <p> <%= f.submit 'Save', class: 'btn btn-info' %> </p>
  <% end %>
</div>
